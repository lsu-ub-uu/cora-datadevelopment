[project]
name = "cora-datadevelopment"
version = "1.0.0"
dependencies = [
    "requests",
    "tqdm",
    "pytest",
    "pytest-cov",
    "pytest-watcher"
]
license = "GPL-3.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
